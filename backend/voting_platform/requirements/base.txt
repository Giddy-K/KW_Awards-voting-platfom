# Runtime dependencies shared by every environment (exact pins).
# Transitive dependencies (requests, django-ipware, etc.) come from
# django-rest-authemail and drf-yasg; pip-audit in CI covers them.
Django==5.2.17  # 5.2 LTS
djangorestframework==3.18.1
django-cors-headers==4.9.0
django-environ==0.14.0
django-filter==26.1
django-rest-authemail==2.1.7
drf-yasg==1.21.15
psycopg2-binary==2.9.13

# Security floors for transitive dependencies that had advisories in the
# previous pins (AUDIT F-14). Not exact pins; they only prevent regressions.
requests>=2.33.0
urllib3>=2.7.0
idna>=3.15
sqlparse>=0.6.0
